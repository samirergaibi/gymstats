export enum Paths {
  ABOUT = '/about',
  EXERCISES = '/exercises',
  LOGIN = '/login',
  NEW_WORKOUT = '/new-workout',
  REGISTER = '/register',
  ROOT = '/',
  WORKOUTS = '/workouts',
}

export enum DBTable {
  EXERCISES = 'exercises',
}
