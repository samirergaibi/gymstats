export enum Paths {
  ABOUT = '/about',
  EXERCISES = '/exercises',
  LOGIN = '/login',
  NEW_WORKOUT = '/new-workout',
  REGISTER = '/register',
  ROOT = '/',
  WORKOUTS = '/workouts',
}

export enum DBTable {
  EXERCISES = 'exercises',
  WORKOUTS = 'workouts',
}

export enum QueryIds {
  WORKOUTS = 'workouts',
  GET_SESSION = 'get-session',
}
